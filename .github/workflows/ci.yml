name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Check config schema
        run: |
          python tools/check_config_schema.py

      - name: Check live dependency boundaries
        run: bash scripts/diagnostics/check_live_deps.sh

      - name: Lint with ruff
        run: |
          ruff check . --output-format=github
          ruff format . --check

      - name: Build dashboard
        run: |
          cd paper_trading/dashboard
          npm ci && npm run build

      - name: Run tests
        run: |
          PYTHONPATH=$PYTHONPATH:. python -m pytest tests/ -v --tb=short

      - name: Run tests with coverage
        run: |
          PYTHONPATH=$PYTHONPATH:. python -m pytest tests/ \
            --cov=. --cov-report=xml --cov-report=term-missing \
            --cov-fail-under=70 -v --tb=short

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          file: ./coverage.xml
          fail_ci_if_error: false
